{
  "name": "spider-chat",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo dev",
    "build": "echo build",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}